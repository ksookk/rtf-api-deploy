<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>51323221-32f3-4e62-ab9e-709bcaad83e0</groupId>
	<artifactId>rtf-api-deploy</artifactId>
    <version>1.0.0</version>
    <packaging>mule-application</packaging>

	<name>rtf-api-deploy</name>

    <properties>
    
	    <!-- Project Properties -->
	    <app.version>1.0.0</app.version>
	        <!-- RAML version -->
	    <jenkins.version>0</jenkins.version>
	        <!-- Do not modify -->
	    <mule.env>DEV-RTF</mule.env>
	        <!-- Do not modify -->
	    <organization.id>51323221-32f3-4e62-ab9e-709bcaad83e0</organization.id>
	    
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

	    <!-- URL Properties -->
	    <app.url.name>rtf-api-deploy</app.url.name>
	    <app.url.version>v1</app.url.version>
	    <visualizer.layer>System</visualizer.layer>
	
	    <!-- Default Allocation Resources DO NOT CHANGE -->
	    <resources.cpu.reserved>150m</resources.cpu.reserved>
	    <resources.cpu.limit>1000m</resources.cpu.limit>
	    <resources.memory.reserved>1500Mi</resources.memory.reserved>
	    <resources.replicas>2</resources.replicas>
	    <deploymentSettings.clustered>false</deploymentSettings.clustered>
	    <deploymentSettings.enforceDeployingReplicasAcrossNodes>false</deploymentSettings.enforceDeployingReplicasAcrossNodes>
	    <deploymentSettings.lastMileSecurity>false</deploymentSettings.lastMileSecurity>
	    <deploymentSettings.updateStrategy>rolling</deploymentSettings.updateStrategy>

		<app.runtime>4.3.0</app.runtime>
		<mule.maven.plugin.version>3.4.2</mule.maven.plugin.version>
	    <munit.version>2.3.1</munit.version>
	
	    <!-- SCM properties -->
	    <maven.release.plugin.version>2.5.3</maven.release.plugin.version>
	    <project.scm.id>XXX</project.scm.id>

	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
				<version>3.0.0</version>
			</plugin>
			<plugin>
				<groupId>org.mule.tools.maven</groupId>
				<artifactId>mule-maven-plugin</artifactId>
				<version>${mule.maven.plugin.version}</version>
				<extensions>true</extensions>
		        <configuration>
		          <runtimeFabricDeployment>
		            <uri>https://anypoint.mulesoft.com</uri>
		            <muleVersion>${app.runtime}</muleVersion>
		            <server>AnypointRepository</server>
		            <applicationName>${project.artifactId}</applicationName>
		            <target>${runtime.fabric.name}</target>
		            <environment>${mule.env}</environment>
		            <provider>MC</provider>
		            <replicas>${resources.replicas}</replicas>
		            <properties>
		              <anypoint.platform.client_id>${anypoint.platform.client.id}</anypoint.platform.client_id>
		              <anypoint.platform.client_secret>${anypoint.platform.client.secret}</anypoint.platform.client_secret>
		              <mule.env>${mule.env}</mule.env>
		              <mule.encryption.key>${mule.encryption.key}</mule.encryption.key>
		              <anypoint.platform.visualizer.layer>${visualizer.layer}</anypoint.platform.visualizer.layer>
		              <mule.properties>${mule.properties}</mule.properties>
		              <jenkins.version>${jenkins.version}</jenkins.version>
		              <api.id>${api.id}</api.id>
		            </properties>
		            <deploymentSettings>
		              <resources>
		                <cpu>
		                  <reserved>${resources.cpu.reserved}</reserved>
		                  <limit>${resources.cpu.limit}</limit>
		                </cpu>
		                <memory>
		                  <reserved>${resources.memory.reserved}</reserved>
		                </memory>
		              </resources>
		              <http>
		                <inbound>
		                  <publicUrl>${app.url}/${app.url.name}/${app.url.version}</publicUrl>
		                </inbound>
		              </http>
		              <clustered>${deploymentSettings.clustered}</clustered>
		              <enforceDeployingReplicasAcrossNodes>${deploymentSettings.enforceDeployingReplicasAcrossNodes}</enforceDeployingReplicasAcrossNodes>
		              <lastMileSecurity>${deploymentSettings.lastMileSecurity}</lastMileSecurity>
		              <updateStrategy>${deploymentSettings.updateStrategy}</updateStrategy>
		            </deploymentSettings>
		          </runtimeFabricDeployment>
		          <classifier>mule-application</classifier>
		        </configuration>
 			</plugin>
		    <plugin>
		        <groupId>com.mulesoft.munit.tools</groupId>
		        <artifactId>munit-maven-plugin</artifactId>
		        <version>${munit.version}</version>
		        <executions>
		          <execution>
		            <id>test</id>
		            <phase>test</phase>
		            <goals>
		              <goal>test</goal>
		              <goal>coverage-report</goal>
		            </goals>
		          </execution>
		        </executions>
		        <configuration>
		          <coverage>
		            <runCoverage>true</runCoverage>
		            <failBuild>true</failBuild>
		            <requiredApplicationCoverage>75</requiredApplicationCoverage>
		            <formats>
		              <format>json</format>
		              <format>html</format>
		            </formats>
		            <ignoreFiles>
		              <ignoreFile>error-handler.xml</ignoreFile>
		              <ignoreFile>module-logger.xml</ignoreFile>
		            </ignoreFiles>
		          </coverage>
		        </configuration>
		      </plugin>
		      <plugin>
		        <groupId>org.apache.maven.plugins</groupId>
		        <artifactId>maven-release-plugin</artifactId>
		        <version>${maven.release.plugin.version}</version>
		      </plugin>
		</plugins>
	</build>

	<dependencies>
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-http-connector</artifactId>
            <version>1.5.22</version>
            <classifier>mule-plugin</classifier>
        </dependency>
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-sockets-connector</artifactId>
            <version>1.2.0</version>
            <classifier>mule-plugin</classifier>
        </dependency>
		<dependency>
            <groupId>org.mule.modules</groupId>
            <artifactId>mule-apikit-module</artifactId>
            <version>1.3.16</version>
            <classifier>mule-plugin</classifier>
        </dependency>
    </dependencies>

	<repositories>
          <repository>
            <id>anypoint-exchange-v2</id>
            <name>Anypoint Exchange</name>
            <url>https://maven.anypoint.mulesoft.com/api/v2/maven</url>
            <layout>default</layout>
        </repository>
        <repository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <id>mulesoft-releases</id>
            <name>mulesoft release repository</name>
            <layout>default</layout>
            <url>https://repository.mulesoft.org/releases/</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

	<!-- to publish assets to Exchange -->
	<distributionManagement>
		<repository>
			<id>AnypointRepository</id>
			<name>Exchange Repository</name>
			<url>https://maven.anypoint.mulesoft.com/api/v2/organizations/${organization.id}/maven</url>
			<layout>default</layout>
		</repository>		
	</distributionManagement>

</project>
